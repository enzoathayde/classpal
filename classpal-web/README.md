# classpal-web

Frontend React + TypeScript (Vite, pnpm) do ClassPal.

```bash
pnpm install
pnpm dev
```

Variáveis em `.env`:

- `VITE_USUARIOS_URL` — default `http://localhost:8081`
- `VITE_ALUNOS_URL` — default `http://localhost:8082`
